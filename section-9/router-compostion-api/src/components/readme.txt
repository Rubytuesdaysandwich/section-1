<!-- was part of vue.app
scoped slots and slots

 <!-- <badge-list></badge-list>
    <user-info
      :full-name="activeUser.name"
      :info-text="activeUser.description"
      :role="activeUser.role"
    ></user-info>
    <course-goals #default="slotProps"
      ><!--this way of writing this scoped slot only works if its from one slotprops -->

    <!-- scoped slots -->
    <!--//!slotProps key word -->
    <!-- <template #default="slotProps"> -->
    <!-- .item equal to the prop on course goals component with :item='goal' -->
    <!-- <h2>{{ slotProps.item }}</h2> -->
    <!-- slotProps["another-prop"] only valid with square bracket notation  -->
    <!-- <p>{{ slotProps["helloProp"] }}</p> -->
    <!--//?{{ slotProps["helloProp"] }} does not seem to work using a dash like in the video  -->
    <!-- </template> -->
    </course-goals> -->
